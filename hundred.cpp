#include <bits/stdc++.h>
using namespace std;

int main(){
    // Read in number of values
    int testcases;
    cin >> testcases;

    // Loop over the tests
    for(int curtest = 0; curtest < testcases; curtest++){
        // Read in the number
        int x;
        cin >> x;

        // Output it converted
        cout << x*120<<"\n";
    }
}
